services:
    _defaults:
        autowire: true
        public: false
        autoconfigure: true

    Lle\EntityFileBundle\Service\EntityFileManager:
        arguments:
            - '@parameter_bag'
            - '@doctrine.orm.entity_manager'
            - !tagged_locator { tag: 'flysystem.storage' }
